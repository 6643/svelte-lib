import type { BuildSvelteOptions } from "./build";

/**
 * Svelte project configuration.
 * All fields are optional - defaults are used if not specified.
 *
 * @see {@link https://github.com/your-repo/svelte-builder}
 */
const config: BuildSvelteOptions = {
    // App entry point (relative to project root)
    // appComponent: "src/index.js",
    // HTML document title
    // appTitle: "Svelte App",
    // DOM mount id (must be plain alphanumeric/underscore/hyphen)
    // mountId: "app",
    // Production build output directory
    // outDir: "dist",
    // Static assets directory (served at / in dev, copied to dist in build)
    // assetsDir: "public",
    // Dev server port
    // port: 3000,
    // Include sourcemaps in production build
    // sourcemap: false,
};

export default config;
